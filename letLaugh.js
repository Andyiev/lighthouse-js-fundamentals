let laugh = function(num) {
    let string ="";
    for (let i = 1 ; i <= num ; i++) {
        string = string + "ha";
    }
    return string + "!";
}/* finish the function expression */

console.log(laugh(10));
